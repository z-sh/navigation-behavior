<Events startTimestamp="1563901034024" logVersion="0.11.0.201504031625" osName="Windows 10" osVersion="10.0" lineSeparator="\r\n" numMonitors="1" monitorBounds="[0, 0, 1920, 1080]">
  <Command __id="0" _type="ShellBoundsCommand" bounds="[-8, -8, 1936, 1056]" timestamp="6" />
  <Command __id="1" _type="ShellBoundsCommand" bounds="[0, 0, 0, 0]" timestamp="37536" />
  <Command __id="2" _type="ShellBoundsCommand" bounds="[-8, -8, 1936, 1056]" timestamp="192916" />
  <Command __id="3" _type="RunCommand" kind="Create" projectName="MineSweeper" timestamp="250400" type="Run" />
  <Command __id="4" _type="RunCommand" exitValue="0" kind="Terminate" projectName="MineSweeper" timestamp="255882" type="Run" />
  <Command __id="5" _type="FileOpenCommand" docASTNodeCount="260" docActiveCodeLength="2374" docExpressionCount="151" docLength="3861" projectName="MineSweeper" timestamp="272578">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeper.java]]></filePath>
    <snapshot><![CDATA[/*
BUG REPORT #2
MineSweeper has 3 difficulty levels. The size of the board and the number of the mines are different for each level. 
Sometimes, we end up having a smaller number of mines in the game. 
For an easy game, we want to have 10 mines, but sometimes it is 9, 8, or even less.
*/

package logic;
import javax.swing.JOptionPane;
import javax.swing.UIManager;

import presentation.CustomMenu;
import presentation.MineSweeperGui;

/**
 * Main launcher class for MineSweeper, containing all the menus and general
 * prompts
 *
 * @author  KnightMiner
 */

public class MineSweeper {
    // currently running instance of the game
    private static MineSweeperGui gui;

    /**
     * Primary way to boot the game
     * @param args  Default args (unused)
     */
    
    public static void main(String[] args) {
        // High DPI stuff
        try {
            UIManager.setLookAndFeel(UIManager.getSystemLookAndFeelClassName());
        }
        catch(Exception e) {
            e.printStackTrace();
        }

        // load the help panels
        // this has a side effect of loading the icons for the pieces
        Help.load();

        // start the game on easy
        newGame(Difficulty.EASY);
    }

    /**
     * Creates a new MineSweeper game with one of three default sizes or custom
     * @param difficulty Game difficulty
     */
    
    public static void newGame(Difficulty difficulty) {
        // difficulty switch
        MineSweeperBoard board = null;
        switch(difficulty) {
            case EASY:
                board = new MineSweeperBoard(9, 9, 10, 0);
                break;
            case NORMAL:
                board = new MineSweeperBoard(16, 16, 40, 1);
                break;
            case HARD:
                board = new MineSweeperBoard(30, 16, 99, 3);
                break;
            case CUSTOM:
                board = customGame();
                break;
        }

        startGame(board);
    }

    /**
     * Creates a new game with the custom size
     * @return  A board of the custom size
     */
    
    public static MineSweeperBoard customGame() {
        CustomMenu menu = new CustomMenu();

        int result = JOptionPane.showConfirmDialog(null, menu,
                "Custom", JOptionPane.OK_CANCEL_OPTION,
                JOptionPane.PLAIN_MESSAGE);

        // if they pressed OK
        if(result == JOptionPane.OK_OPTION) {
            return menu.createBoard();
        }
        else {
            return null;
        }
    }

    /**
     * Starts a new game with the specified board
     * @param board  Game board to start
     */
    
    public static void startGame(MineSweeperBoard board) {
        // must be a valid board
        if(board != null) {
            // remove the current gui and define the new one
            if(gui != null) {
                gui.dispose();
            }

            // create a new instance
            gui = new MineSweeperGui(board);
        }
    }

    /**
     * List of buttons for the GUI
     */
    
    public enum Difficulty {
        /** Easy game */
        EASY("Easy"),
        /** Normal game */
        NORMAL("Normal"),
        /** Hard game */
        HARD("Expert"),
        /** Custom size and mines from the GUI */
        CUSTOM("Custom");

        // data
        private String label;

        /**
         * Default constructor
         * @param  Button label
         */
        
        Difficulty(String label) {
            this.label = label;
        }

        /**
         * Gets the label for the difficulty
         * @return  Difficulty name
         */
        
        public String getLabel() {
            return label;
        }
    }
}
]]></snapshot>
  </Command>
  <Command __id="6" _type="SelectTextCommand" caretOffset="1062" docEnd="1446" docOffset="1446" docStart="1439" end="1062" start="1055" timestamp="273036" />
  <Command __id="7" _type="MoveCaretCommand" caretOffset="804" docOffset="1188" timestamp="279957" />
  <Command __id="8" _type="MoveCaretCommand" caretOffset="802" docOffset="1186" timestamp="281068" />
  <Command __id="9" _type="MoveCaretCommand" caretOffset="874" docOffset="1258" timestamp="282228" />
  <Command __id="10" _type="RunCommand" kind="Create" projectName="MineSweeper" timestamp="313809" type="Run" />
  <Command __id="11" _type="RunCommand" exitValue="0" kind="Terminate" projectName="MineSweeper" timestamp="351876" type="Run" />
  <Command __id="12" _type="MoveCaretCommand" caretOffset="1543" docOffset="1927" timestamp="359125" />
  <Command __id="13" _type="MoveCaretCommand" caretOffset="874" docOffset="1258" timestamp="361710" />
  <Command __id="14" _type="MoveCaretCommand" caretOffset="871" docOffset="1255" timestamp="364388" />
  <Command __id="15" _type="MoveCaretCommand" caretOffset="165" docOffset="549" timestamp="383037" />
  <Command __id="16" _type="MoveCaretCommand" caretOffset="7" docOffset="11" timestamp="384820" />
  <Command __id="17" _type="MoveCaretCommand" caretOffset="13" docOffset="17" timestamp="386028" />
  <Command __id="18" _type="MoveCaretCommand" caretOffset="296" docOffset="296" timestamp="389325" />
  <Command __id="19" _type="MoveCaretCommand" caretOffset="294" docOffset="294" timestamp="390972" />
  <Command __id="20" _type="FileOpenCommand" docASTNodeCount="1769" docActiveCodeLength="10590" docExpressionCount="1040" docLength="19616" projectName="MineSweeper" timestamp="416156">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeperBoard.java]]></filePath>
    <snapshot><![CDATA[package logic;

import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.IOException;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;
import java.io.Serializable;
import java.util.*;

import util.Piece;
import util.Space;

/**
 * Contains all the visible pieces in the MineSweeper game
 * 
 * @author KnightMiner
 */

public class MineSweeperBoard implements Serializable {

	/**
	 * Random number generator used to create game seeds Minefields use a sepparate
	 * object created using a specific seed
	 */
	
	public static final Random RANDOM = new Random();

	// resizable
	private transient Queue<Space> update;

	// board dimensions
	private int width, height;
	private MineGenerator data = new MineGenerator();

	private int flagCount;

	// board data
	private Piece[][] pieces;
	private boolean[][] mines;
	private boolean gameOver;
	private boolean victory;

	// determines if the first click happened yet
	private boolean firstClick;

	// determines if we have used our one cheat
	private int cheatsAllowed;
	private int cheats = 0;

	// board seed
	private long seed;

	/**
	 * Creates a new minesweaper board with the specified dimensions
	 * 
	 * @param width     Width of the board
	 * @param height    Height of the board
	 * @param mineCount Total mines
	 * @param cheats    Cheats allowed
	 */
	
	public MineSweeperBoard(int width, int height, int mineCount, int cheats) {
		this.width = width;
		this.height = height;

		this.pieces = new Piece[height][width];
		this.mines = new boolean[height][width];

		// sanity check in case the GUI fails to correct the number
		this.data.mineCount = Math.min(mineCount, width * height - 9);
		this.cheatsAllowed = this.cheats = cheats;

		update = new LinkedList<>();
	}

	/**
	 * Creates a new minesweaper board with the specified dimensions
	 * 
	 * @param width     Width of the board
	 * @param height    Height of the board
	 * @param mineCount Total mines
	 */
	
	public MineSweeperBoard(int width, int height, int mineCount) {
		this(width, height, mineCount, 1);
	}

	/* Main logic */

	/**
	 * Populates the board with mines with a random seed
	 * 
	 * @param space Space clicked
	 */
	
	public void generateMines(Space space) {
		generateMines(RANDOM.nextLong(), space);
	}

	/**
	 * Populates the board with mines
	 * 
	 * @param seed    Seed to generate mines. Two fields with the same size and seed
	 *                will have the same mines locations
	 * @param clicked space clicked when generating the mines
	 */
	
	protected void generateMines(long seed, Space clicked) {
		// store the current seed for saving games
		this.seed = seed;

		// no mines? no work
		if (data.mineCount == 0) {
			return;
		}

		// list of spaces we can still place a mine at
		List<Space> remainingSpaces = new ArrayList<>(Arrays.asList(getAllSpaces()));

		// if we clicked somewhere, remove all nearby spaces
		if (clicked != null) {
			// remove all unclickable spaces
			final int x = clicked.getX(), y = clicked.getY();
			remainingSpaces.removeIf((space) -> {
				return (y - 1) <= space.getY() && space.getY() <= (y + 1) && (x - 1) <= space.getX()
						&& space.getX() <= (x + 1);
			});
		}

		// random object seeded so results can be controlled
		Random random = new Random(seed);

		// place mines
		this.mines = new boolean[height][width];
		for (int i = 0; i < data.mineCount; i++) {
			// just get the linear location of the next mine and place it
			int mine = random.nextInt(remainingSpaces.size());
			Space space = remainingSpaces.get(mine);
			mines[space.getY()][space.getX()] = true;

		}
	}

	/**
	 * Creates a new game with the same dimensions
	 */
	
	public void newGame() {
		// first, regenerate the mines
		this.mines = new boolean[height][width];

		// next, clear game specific data
		resetData();

		// then mark all spaces for a rendering update
		for (Space space : getAllSpaces()) {
			markUpdate(space);
		}
	}

	/**
	 * Restarts the current game
	 */
	
	public void restart() {
		// if we have not clicked yet, do nothing
		// we don't have a seed to restart from
		if (!firstClick) {
			return;
		}

		// reset any relevant data
		resetData();

		// but assume we already clicked so the mines don't change
		firstClick = true;

		// and mark all spaces for an update
		for (Space space : getAllSpaces()) {
			markUpdate(space);
		}
	}

	/**
	 * Saves the board to a binary file
	 * 
	 * @param filename Location of the game
	 */
	
	public void saveGame(String filename) {
		// TODO: better saving logic
		try {
			FileOutputStream file = new FileOutputStream(filename + ".bin");
			ObjectOutputStream stream = new ObjectOutputStream(file);
			stream.writeObject(this);
			stream.flush();
			stream.close();
		} catch (IOException e) {
		}
	}

	/**
	 * Load the board from a binary file
	 * 
	 * @param filename Location of the game
	 * @return The board, or null if invalid
	 */
	
	public static MineSweeperBoard loadGame(String filename) {
		try {
			FileInputStream file = new FileInputStream(filename + ".bin");
			ObjectInputStream stream = new ObjectInputStream(file);
			MineSweeperBoard board = (MineSweeperBoard) stream.readObject();
			stream.close();
			return board;
		} catch (IOException | ClassNotFoundException e) {
			e.printStackTrace();
			return null;
		}
	}

	/**
	 * Resets generic data
	 */
	
	private void resetData() {
		this.pieces = new Piece[height][width];
		gameOver = false;
		firstClick = false;
		cheats = cheatsAllowed;
		flagCount = 0;
		victory = false;
	}

	/**
	 * Ends the game with a loss
	 * 
	 * @param clicked Location clicked causing the loss
	 * @return Mine clicked
	 */
	
	public void loseGame(Space clicked) {
		// set the game to over
		gameOver = true;

		showMines(clicked, false);
	}

	/**
	 * Checks if the game has been won, and sets the appropiate statuses
	 */
	
	public void checkVictory() {
		// check all spaces
		for (Space space : getAllSpaces()) {

			// only spaces without mines need to be checked, we don't care about
			// flags or marks on mine spaces
			if (!isMine(space)) {
				// if the piece is unclicked or not a number, we failed
				Piece piece = getPiece(space);
				if (piece == null || !piece.isNumber()) {
					return;
				}
			}
		}

		// made it through that? we won
		victory = true;
		gameOver = true;

		showMines(null, true);
	}

	/**
	 * Shows all the mines on the board
	 * 
	 * @param space   Space that was clicked
	 * @param victory Determines if the game was won or lost
	 */
	
	private void showMines(Space clicked, boolean victory) {
		// set the mine color based on victory
		Piece mine = victory ? Piece.MINE_GREEN : Piece.MINE;

		// set the clicked space to red
		if (clicked != null && isMine(clicked)) {
			setPiece(clicked, Piece.MINE_RED);
		}

		// show mines on the board
		for (Space space : getAllSpaces()) {
			if (space.equals(clicked)) {
				continue;
			}
			Piece piece = getPiece(space);
			if (isMine(space)) {
				// but only on empty or marks
				if (piece == null || piece.isMark()) {
					setPiece(space, mine);
				}
			} else {
				// if the space has a flag but no mine, mark it as such
				if (piece == Piece.FLAG) {
					setPiece(space, Piece.FLAG_NOT);
				}
			}
		}
	}

	/* Clicking */

	/**
	 * Handle standard game clicks
	 * 
	 * @param space  Space clicked
	 * @param action Type of click
	 */
	
	public void handleClick(Space space, ClickAction action) {
		// stop playing when the game is over
		if (gameOver) {
			return;
		}
		// simply passes it along to the dedicated function
		switch (action) {
		case DEFAULT:
			handleDefaultClick(space);
			break;
		case FLAG:
			handleFlagClick(space);
			break;
		case MARK:
			handleMarkClick(space);
			break;
		case CHEAT:
			handleCheatClick(space);
			break;
		}
	}

	/**
	 * Space clicking core, does the actual clicking action
	 * 
	 * @param space Space clicked
	 */
	
	private void clickSpace(Space space) {
		// if the space is not replaceable, stop now
		// called here so we don't click flags as they are mines
		if (!isReplaceable(space)) {
			return;
		}

		// if the space is a mine, we lose
		if (isMine(space)) {
			loseGame(space);
		}

		// otherwise do a normal click
		else {
			// sets the piece to the number of surrounding mines
			Piece newPiece = getNumber(space);
			setPiece(space, newPiece);

			// if the piece is 0, update surrounding pieces as none of them are mines
			// this won't eternally recur since this space is no longer clickable
			if (newPiece == Piece.N0) {
				for (Space neighbor : getNeighbors(space)) {
					clickSpace(neighbor);
				}
			}
		}
	}

	/**
	 * Default clicks, clicks a space and displays a number or loses the game
	 * 
	 * @param space Space clicked
	 */
	
	private void handleDefaultClick(Space space) {
		// first click should generate the minefield
		if (!firstClick) {
			firstClick(space);
		}

		// if the space cannot be replaced then stop here
		// as we run this function on every click
		if (isNumber(space)) {
			// if the number of flags around this space is the same as the number
			Space[] neighbors = getNeighbors(space);

			// count the flags around the piece
			int flags = 0;
			for (Space neighbor : neighbors) {
				if (getPiece(neighbor) == Piece.FLAG) {
					flags++;
				}
			}

			// same number of flags as the piece number?
			if (flags == getPiece(space).getNumber()) {
				// click all the spaces. This is a risky move if a flag is wrong
				for (Space neighbor : neighbors) {
					clickSpace(neighbor);
				}
			}
		}
		// otherwise if not a number, just click the space
		else {
			clickSpace(space);
		}

		// check just once at the end of clicking
		checkVictory();
	}

	/**
	 * Handle the first click in the game. This click will generate the minefield as
	 * it cannot be on a mine
	 * 
	 * @param space Space clicked
	 */
	
	private void firstClick(Space space) {
		generateMines(space);
		firstClick = true;
	}

	/**
	 * Flag clicks, essentially click once to add
	 * 
	 * @param space Space clicked
	 */
	
	private void handleFlagClick(Space space) {
		// if already a flag, set a mark
		Piece piece = getPiece(space);
		if (piece == Piece.FLAG) {
			setPiece(space, Piece.MARK_RED);
			flagCount--;
		}
		// remove marks
		else if (piece != null && piece.isMark()) {
			setPiece(space, null);
		}
		// otherwise if we can add one
		else if (isReplaceable(space)) {
			if(flagCount < data.mineCount) {
				setPiece(space, Piece.FLAG);
				flagCount++;
			}
		}
	}

	/**
	 * Mark clicks, changes the color of a mark
	 * 
	 * @param space Space clicked
	 */
	
	private void handleMarkClick(Space space) {
		// tobble the mark
		Piece piece = getPiece(space);
		if (piece == Piece.MARK_RED) {
			setPiece(space, Piece.MARK_GREEN);
		}
		// remove marks
		else if (piece == Piece.MARK_GREEN) {
			setPiece(space, Piece.MARK_BLUE);
		}
		// otherwise if we can add one
		else if (piece == Piece.MARK_BLUE) {
			setPiece(space, Piece.MARK_RED);
		}

		// if its a flag, remove it
		else if (piece == Piece.FLAG) {
			setPiece(space, Piece.MARK_RED);
			flagCount--;
		}

		// essentially all that is left here is null, but still
		else if (isReplaceable(space)) {
			setPiece(space, Piece.MARK_RED);
		}
	}

	/**
	 * Cheat clicks, click a space and it will show a "ghost" bomb if one exists
	 * 
	 * @param space Space clicked
	 */
	
	private void handleCheatClick(Space space) {
		// already ran, don't run again
		if (!canCheat()) {
			return;
		}

		// no mines if we have not clicked
		if (!firstClick) {
			// you cheated for the first click, useless move as now we don't care
			// about click protection
			firstClick(null);
		}

		// only marks and empty spaces can be cheated, to save accidental clicks
		Piece piece = getPiece(space);
		if (piece != null && !piece.isMark()) {
			return;
		}

		// if its a mine, show that
		if (isMine(space)) {
			setPiece(space, Piece.MINE_GREEN);
		}
		// otherwise click the space as we know its safe
		else {
			clickSpace(space);
		}

		// use a cheat
		cheats--;

		// need to check, since it could be the last click
		checkVictory();
	}

	/* Helper functions */

	/**
	 * Gets a list of all spaces
	 * 
	 * @return a list of all spaces
	 */
	
	public Space[] getAllSpaces() {
		Space[] spaces = new Space[width * height];

		// loop through all spaces, adding them to an array
		int i = 0;
		for (int y = 0; y < height; y++) {
			for (int x = 0; x < width; x++) {
				spaces[i] = new Space(x, y);
				i++;
			}
		}

		return spaces;
	}

	/**
	 * Helper function to get the coordinates of all 8 neighbors to the space Note
	 * that some may be invalid spaces
	 * 
	 * @param space Space around which to get neighbors
	 * @return an array of spaces of the 8 neighbors, or less on edges
	 */
	
	public Space[] getNeighbors(Space space) {
		// if not valid, return no neighbors
		if (!isValid(space)) {
			return new Space[0];
		}

		// array cannot be bigger than 8
		Space[] neighbors = new Space[8];
		int x = space.getX();
		int y = space.getY();

		// 2D loop from x - 1, y - 1 up to x + 1, y + 1
		int neighborCount = 0;
		for (int i = -1; i <= 1; i++) {
			for (int j = -1; j <= 1; j++) {
				// skip offset of 0,0 as that is the input space
				if (i == 0 && j == 0) {
					continue;
				}

				// make sure the space is valid before returning it
				Space neighbor = new Space(x + i, y + j);
				if (isValid(neighbor)) {
					neighbors[neighborCount] = neighbor;
					neighborCount++;
				}
			}
		}

		return neighbors;
	}

	/**
	 * Marks a space as needing update by the graphics display
	 * 
	 * @param space space needing an update
	 */
	
	public void markUpdate(Space space) {
		if (!update.contains(space)) {
			update.add(space);
		}
	}

	/**
	 * Returns the number to display on the space after clicking it Does not take
	 * bombs on this space into account, check isMine() first
	 * 
	 * @param space Space to check
	 * @return The number of bombs around this space
	 */
	
	public Piece getNumber(Space space) {
		// no space? no return
		if (!isValid(space)) {
			return null;
		}

		int count = 0;
		// check all the neighbors for a bomb
		for (Space neighbor : getNeighbors(space)) {
			// if they have one, add to the running total
			if (isMine(neighbor)) {
				count++;
			}
		}

		// return the specific piece
		return Piece.fromNumber(count);
	}

	/* Getters, setters, and basic return logic */

	/**
	 * Gets the piece on the specified space
	 * 
	 * @param space Space to check for a piece
	 * @return the piece on the specified space
	 */
	
	public Piece getPiece(Space space) {
		// invalid gets null
		if (!isValid(space)) {
			return null;
		}

		// otherwise go with the contained piece
		return pieces[space.getY()][space.getX()];
	}

	/**
	 * Placed a piece on a space
	 * 
	 * @param space Space to place the piece
	 * @param piece Piece to place on the space
	 */
	
	public void setPiece(Space space, Piece piece) {
		// must be valid
		if (isValid(space)) {
			pieces[space.getY()][space.getX()] = piece;
			markUpdate(space);
		}
	}

	/**
	 * Determines if a space contains a bomb
	 * 
	 * @param space Space to check
	 * @return True if the space contains a bomb
	 */
	
	public boolean isMine(Space space) {
		// not a valid space?
		if (!isValid(space)) {
			return false;
		}

		return mines[space.getY()][space.getX()];
	}

	/**
	 * Determines if a space appears enabled, or pressed up
	 * 
	 * @param space Space to check
	 * @return True if the space appears enabled
	 */
	
	public boolean isEnabled(Space space) {
		if (!isValid(space)) {
			return false;
		}

		Piece piece = getPiece(space);

		// if the space is empty, it pops up
		if (piece == null) {
			return true;
		}

		// otherwise go based on the contained value
		return piece.isEnabled();
	}

	/**
	 * Determines if the contents of a space can be replaced
	 * 
	 * @param space Space clicked
	 */
	
	private boolean isReplaceable(Space space) {
		// invalid spaces are always false
		if (!isValid(space)) {
			return false;
		}

		Piece piece = getPiece(space);

		// if the space is empty, it can be clicked
		if (piece == null) {
			return true;
		}

		// otherwise go based on the contained value
		return piece.isReplaceable();
	}

	/**
	 * Determines if the space is a number
	 * 
	 * @param space Space clicked
	 */
	
	private boolean isNumber(Space space) {
		// invalid spaces are always false
		if (!isValid(space)) {
			return false;
		}

		Piece piece = getPiece(space);

		// if the space is empty, it is not a number
		if (piece == null) {
			return false;
		}

		// otherwise go based on the contained value
		return piece.isNumber();
	}

	/* Data */

	/**
	 * Gets the width of the board
	 * 
	 * @return The width of the board
	 */
	
	public int getWidth() {
		return width;
	}

	/**
	 * Gets the height of the board
	 * 
	 * @return The height of the board
	 */
	
	public int getHeight() {
		return height;
	}

	/**
	 * Gets the number of mines left on the board
	 * 
	 * @return number of mines remaining
	 */
	
	public int getRemainingMines() {
		return data.mineCount - flagCount;
	}

	/**
	 * Checks if the player has used their cheat
	 * 
	 * @return if the player has used their cheat
	 */
	
	public boolean canCheat() {
		return cheats > 0;
	}

	/**
	 * Checks how many cheats the player has left
	 * 
	 * @return remaining cheats
	 */
	
	public int getCheats() {
		return cheats;
	}

	/**
	 * Checks if the game ended
	 * 
	 * @return true if the game ended
	 */
	
	public boolean gameOver() {
		return gameOver;
	}

	/**
	 * Checks if the player won the game
	 * 
	 * @return true if the player won the game
	 */
	
	public boolean hasWon() {
		return victory;
	}

	/**
	 * Checks if a space is within the bounds of this board
	 * 
	 * @param space Space to check
	 * @return true if the space is within this board
	 */
	
	public boolean isValid(Space space) {
		// no space?
		if (space == null) {
			return false;
		}

		// first, check if the space is entirely invalid
		if (space.getX() < 0 || space.getY() < 0) {
			return false;
		}

		// next, check if this is outside of this board
		if (space.getY() >= height || space.getX() >= width) {
			return false;
		}

		// fits in both? return true then
		return true;
	}

	/**
	 * Gets a list of spaces pending updates to update the button displays
	 * 
	 * @return a list of spaces needing an update
	 */
	
	public Queue<Space> getUpdates() {
		return update;
	}

	/**
	 * Actions that can be performed on click. Used by buttons to pass along a
	 * constant rather than a list of modifiers.
	 */
	
	public enum ClickAction {
		/** Normal click */
		DEFAULT,
		/** Placing a flag or mark */
		FLAG,
		/** Placing a mark of different colors */
		MARK,
		/** Safely clicking a mine */
		CHEAT;
	}
}
]]></snapshot>
  </Command>
  <Command __id="21" _type="MoveCaretCommand" caretOffset="0" docOffset="0" timestamp="416239" />
  <Command __id="22" _type="FileOpenCommand" docASTNodeCount="260" docActiveCodeLength="2374" docExpressionCount="151" docLength="3861" projectName="MineSweeper" timestamp="434144">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeper.java]]></filePath>
  </Command>
  <Command __id="23" _type="MoveCaretCommand" caretOffset="294" docOffset="294" timestamp="434203" />
  <Command __id="24" _type="FileOpenCommand" docASTNodeCount="1769" docActiveCodeLength="10590" docExpressionCount="1040" docLength="19616" projectName="MineSweeper" timestamp="452652">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeperBoard.java]]></filePath>
  </Command>
  <Command __id="25" _type="MoveCaretCommand" caretOffset="0" docOffset="0" timestamp="452689" />
  <Command __id="26" _type="FileOpenCommand" docASTNodeCount="260" docActiveCodeLength="2374" docExpressionCount="151" docLength="3861" projectName="MineSweeper" timestamp="489097">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeper.java]]></filePath>
  </Command>
  <Command __id="27" _type="MoveCaretCommand" caretOffset="294" docOffset="294" timestamp="489135" />
  <Command __id="28" _type="FileOpenCommand" docASTNodeCount="1769" docActiveCodeLength="10590" docExpressionCount="1040" docLength="19616" projectName="MineSweeper" timestamp="525001">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeperBoard.java]]></filePath>
  </Command>
  <Command __id="29" _type="MoveCaretCommand" caretOffset="0" docOffset="0" timestamp="525038" />
  <Command __id="30" _type="MoveCaretCommand" caretOffset="2538" docOffset="2765" timestamp="553372" />
  <Command __id="31" _type="MoveCaretCommand" caretOffset="1620" docOffset="1847" timestamp="604868" />
  <Command __id="32" _type="MoveCaretCommand" caretOffset="1580" docOffset="1807" timestamp="605860" />
  <Command __id="33" _type="MoveCaretCommand" caretOffset="1534" docOffset="1761" timestamp="607908" />
  <Command __id="34" _type="MoveCaretCommand" caretOffset="1403" docOffset="1630" timestamp="615357" />
  <Command __id="35" _type="MoveCaretCommand" caretOffset="1534" docOffset="1761" timestamp="632029" />
  <Command __id="36" _type="MoveCaretCommand" caretOffset="1530" docOffset="1757" timestamp="638572" />
  <Command __id="37" _type="MoveCaretCommand" caretOffset="1316" docOffset="1543" timestamp="658132" />
  <Command __id="38" _type="MoveCaretCommand" caretOffset="2155" docOffset="2382" timestamp="676853" />
  <Command __id="39" _type="MoveCaretCommand" caretOffset="2611" docOffset="2838" timestamp="690668" />
  <Command __id="40" _type="MoveCaretCommand" caretOffset="3258" docOffset="3485" timestamp="724892" />
  <Command __id="41" _type="MoveCaretCommand" caretOffset="3350" docOffset="3577" timestamp="725548" />
  <Command __id="42" _type="MoveCaretCommand" caretOffset="3304" docOffset="3531" timestamp="726612" />
  <Command __id="43" _type="MoveCaretCommand" caretOffset="3425" docOffset="3652" timestamp="732444" />
  <Command __id="44" _type="MoveCaretCommand" caretOffset="3516" docOffset="3743" timestamp="739388" />
  <Command __id="45" _type="FileOpenCommand" docASTNodeCount="43" docActiveCodeLength="235" docExpressionCount="27" docLength="235" projectName="MineSweeper" timestamp="748235">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineGenerator.java]]></filePath>
    <snapshot><![CDATA[package logic;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Random;

import util.Space;

public class MineGenerator {
	public int mineCount;

	public MineGenerator() {
	}
}]]></snapshot>
  </Command>
  <Command __id="46" _type="MoveCaretCommand" caretOffset="0" docOffset="0" timestamp="748305" />
  <Command __id="47" _type="FileOpenCommand" docASTNodeCount="1769" docActiveCodeLength="10590" docExpressionCount="1040" docLength="19616" projectName="MineSweeper" timestamp="754450">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeperBoard.java]]></filePath>
  </Command>
  <Command __id="48" _type="MoveCaretCommand" caretOffset="3516" docOffset="3743" timestamp="754501" />
  <Command __id="49" _type="MoveCaretCommand" caretOffset="2107" docOffset="2334" timestamp="768245" />
  <Command __id="50" _type="MoveCaretCommand" caretOffset="2155" docOffset="2382" timestamp="769501" />
  <Command __id="51" _type="MoveCaretCommand" caretOffset="2107" docOffset="2334" timestamp="777572" />
  <Command __id="52" _type="MoveCaretCommand" caretOffset="3194" docOffset="3421" timestamp="825228" />
  <Command __id="53" _type="MoveCaretCommand" caretOffset="3258" docOffset="3485" timestamp="831348" />
  <Command __id="54" _type="MoveCaretCommand" caretOffset="3298" docOffset="3525" timestamp="850925" />
  <Command __id="55" _type="MoveCaretCommand" caretOffset="3304" docOffset="3531" timestamp="851429" />
  <Command __id="56" _type="MoveCaretCommand" caretOffset="3470" docOffset="3697" timestamp="861244" />
  <DocumentChange __id="57" _type="Delete" docASTNodeCount="1769" docActiveCodeLength="10585" docExpressionCount="1040" docLength="19611" endLine="139" length="5" offset="3697" repeat="2" startLine="138" timestamp="971989" timestamp2="973445">
    <text><![CDATA[
			]]></text>
  </DocumentChange>
  <Command __id="58" _type="EclipseCommand" commandID="org.eclipse.ui.edit.delete" timestamp="972000" />
  <DocumentChange __id="60" _type="Insert" docASTNodeCount="1769" docActiveCodeLength="10588" docExpressionCount="1040" docLength="19614" length="5" offset="3697" timestamp="973449">
    <text><![CDATA[
			]]></text>
  </DocumentChange>
  <Command __id="61" _type="InsertStringCommand" timestamp="973460">
    <data><![CDATA[]]></data>
  </Command>
  <Command __id="62" _type="MoveCaretCommand" caretOffset="3470" docOffset="3697" timestamp="974852" />
  <Command __id="63" _type="MoveCaretCommand" caretOffset="3516" docOffset="3743" timestamp="981780" />
  <Command __id="64" _type="MoveCaretCommand" caretOffset="3470" docOffset="3697" timestamp="982686" />
  <Command __id="65" _type="MoveCaretCommand" caretOffset="3516" docOffset="3743" timestamp="994533" />
  <Command __id="66" _type="MoveCaretCommand" caretOffset="3523" docOffset="3750" timestamp="995388" />
  <Command __id="67" _type="MoveCaretCommand" caretOffset="3518" docOffset="3745" timestamp="996677" />
  <DocumentChange __id="68" _type="Insert" docASTNodeCount="1769" docActiveCodeLength="10593" docExpressionCount="1040" docLength="19619" length="3" offset="3745" timestamp="997587">
    <text><![CDATA[			]]></text>
  </DocumentChange>
  <Command __id="69" _type="InsertStringCommand" timestamp="997589">
    <data><![CDATA[	]]></data>
  </Command>
  <DocumentChange __id="70" _type="Insert" docASTNodeCount="1771" docActiveCodeLength="10609" docExpressionCount="1041" docLength="19635" length="16" offset="3748" repeat="16" timestamp="1002910" timestamp2="1006327">
    <text><![CDATA[remainingSpaces.]]></text>
  </DocumentChange>
  <Command __id="71" _type="InsertStringCommand" repeat="16" timestamp="1002913" timestamp2="1006331">
    <data><![CDATA[remainingSpaces.]]></data>
  </Command>
  <Command __id="102" _type="AssistCommand" assist_type="CONTENT_ASSIST" auto_activated="true" start_end="START" timestamp="1006368" />
  <DocumentChange __id="103" _type="Insert" docASTNodeCount="1773" docActiveCodeLength="10611" docExpressionCount="1043" docLength="19637" length="2" offset="3764" repeat="2" timestamp="1008881" timestamp2="1009020">
    <text><![CDATA[er]]></text>
  </DocumentChange>
  <DocumentChange __id="105" _type="Insert" docASTNodeCount="1773" docActiveCodeLength="10612" docExpressionCount="1043" docLength="19638" length="1" offset="3766" timestamp="1012984">
    <text><![CDATA[a]]></text>
  </DocumentChange>
  <DocumentChange __id="106" _type="Delete" docASTNodeCount="1771" docActiveCodeLength="10609" docExpressionCount="1041" docLength="19635" endLine="140" length="3" offset="3764" repeat="3" startLine="140" timestamp="1013848" timestamp2="1014183">
    <text><![CDATA[era]]></text>
  </DocumentChange>
  <Command __id="109" _type="AssistCommand" assist_type="CONTENT_ASSIST" auto_activated="false" start_end="END" timestamp="1030245" />
  <DocumentChange __id="110" _type="Insert" docASTNodeCount="1773" docActiveCodeLength="10617" docExpressionCount="1043" docLength="19643" length="8" offset="3764" repeat="7" timestamp="1030951" timestamp2="1033170">
    <text><![CDATA[remove()]]></text>
  </DocumentChange>
  <Command __id="111" _type="InsertStringCommand" repeat="7" timestamp="1030954" timestamp2="1033191">
    <data><![CDATA[remove(]]></data>
  </Command>
  <Command __id="124" _type="MoveCaretCommand" caretOffset="3541" docOffset="3768" timestamp="1038973" />
  <Command __id="125" _type="MoveCaretCommand" caretOffset="3544" docOffset="3771" timestamp="1042917" />
  <DocumentChange __id="126" _type="Insert" docASTNodeCount="1774" docActiveCodeLength="10621" docExpressionCount="1044" docLength="19647" length="4" offset="3771" repeat="4" timestamp="1048521" timestamp2="1050059">
    <text><![CDATA[mine]]></text>
  </DocumentChange>
  <Command __id="127" _type="InsertStringCommand" repeat="4" timestamp="1048524" timestamp2="1050060">
    <data><![CDATA[mine]]></data>
  </Command>
  <Command __id="134" _type="EclipseCommand" commandID="eventLogger.styledTextCommand.COLUMN_NEXT" timestamp="1051414" />
  <DocumentChange __id="135" _type="Insert" docASTNodeCount="1774" docActiveCodeLength="10622" docExpressionCount="1044" docLength="19648" length="1" offset="3776" timestamp="1051828">
    <text><![CDATA[;]]></text>
  </DocumentChange>
  <Command __id="136" _type="InsertStringCommand" timestamp="1051833">
    <data><![CDATA[;]]></data>
  </Command>
  <Command __id="137" _type="MoveCaretCommand" caretOffset="3555" docOffset="3782" timestamp="1054493" />
  <Command __id="138" _type="MoveCaretCommand" caretOffset="3550" docOffset="3777" timestamp="1058572" />
  <Command __id="139" _type="RunCommand" kind="Create" projectName="MineSweeper" timestamp="1065108" type="Run" />
  <Command __id="140" _type="RunCommand" exitValue="0" kind="Terminate" projectName="MineSweeper" timestamp="1075726" type="Run" />
  <Command __id="141" _type="FileOpenCommand" docASTNodeCount="260" docActiveCodeLength="2374" docExpressionCount="151" docLength="3861" projectName="MineSweeper" timestamp="1097182">
    <filePath><![CDATA[C:\Users\Eye-tracking\git\MineSweeper-Experiment\src\logic\MineSweeper.java]]></filePath>
  </Command>
  <Command __id="142" _type="MoveCaretCommand" caretOffset="294" docOffset="294" timestamp="1097216" />
  <Command __id="143" _type="MoveCaretCommand" caretOffset="1546" docOffset="1649" timestamp="1118909" />
  <DocumentChange __id="144" _type="Delete" docASTNodeCount="260" docActiveCodeLength="2373" docExpressionCount="151" docLength="3860" endLine="57" length="1" offset="1648" startLine="57" timestamp="1119948">
    <text><![CDATA[1]]></text>
  </DocumentChange>
  <Command __id="145" _type="EclipseCommand" commandID="eventLogger.styledTextCommand.DELETE_PREVIOUS" timestamp="1119952" />
  <Command __id="146" _type="EclipseCommand" commandID="eventLogger.styledTextCommand.PAGE_DOWN" timestamp="1120593" />
  <Command __id="147" _type="MoveCaretCommand" caretOffset="1545" docOffset="1648" timestamp="1131108" />
  <DocumentChange __id="148" _type="Insert" docASTNodeCount="260" docActiveCodeLength="2374" docExpressionCount="151" docLength="3861" length="1" offset="1648" timestamp="1131837">
    <text><![CDATA[3]]></text>
  </DocumentChange>
  <Command __id="149" _type="InsertStringCommand" timestamp="1131840">
    <data><![CDATA[3]]></data>
  </Command>
  <Command __id="150" _type="MoveCaretCommand" caretOffset="1576" docOffset="1679" timestamp="1132599" />
  <Command __id="151" _type="MoveCaretCommand" caretOffset="1552" docOffset="1655" timestamp="1133053" />
  <Command __id="152" _type="RunCommand" kind="Create" projectName="MineSweeper" timestamp="1136190" type="Run" />
  <Command __id="153" _type="RunCommand" exitValue="0" kind="Terminate" projectName="MineSweeper" timestamp="1141202" type="Run" />
  <Command __id="154" _type="MoveCaretCommand" caretOffset="1546" docOffset="1649" timestamp="1142276" />
  <DocumentChange __id="155" _type="Delete" docASTNodeCount="260" docActiveCodeLength="2373" docExpressionCount="151" docLength="3860" endLine="57" length="1" offset="1648" startLine="57" timestamp="1142989">
    <text><![CDATA[3]]></text>
  </DocumentChange>
  <Command __id="156" _type="EclipseCommand" commandID="eventLogger.styledTextCommand.DELETE_PREVIOUS" timestamp="1142994" />
  <Command __id="157" _type="EclipseCommand" commandID="org.eclipse.ui.edit.text.goto.lineEnd" timestamp="1143366" />
  <Command __id="158" _type="MoveCaretCommand" caretOffset="1545" docOffset="1648" timestamp="1144389" />
  <DocumentChange __id="159" _type="Insert" docASTNodeCount="260" docActiveCodeLength="2374" docExpressionCount="151" docLength="3861" length="1" offset="1648" timestamp="1145175">
    <text><![CDATA[1]]></text>
  </DocumentChange>
  <Command __id="160" _type="InsertStringCommand" timestamp="1145177">
    <data><![CDATA[1]]></data>
  </Command>
  <Command __id="161" _type="MoveCaretCommand" caretOffset="1552" docOffset="1655" timestamp="1145630" />
  <Command __id="162" _type="RunCommand" kind="Create" projectName="MineSweeper" timestamp="1151546" type="Run" />
  <Command __id="163" _type="RunCommand" exitValue="0" kind="Terminate" projectName="MineSweeper" timestamp="1159990" type="Run" />
  <Command __id="164" _type="ShellBoundsCommand" bounds="[-8, -8, 1936, 1056]" timestamp="1190499" />
</Events>
